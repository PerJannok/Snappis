<header class="navigation">
  <div class="menu-wrapper">
    <div class="logo"><%= link_to "Reviewsite", root_path, :class => 'brand' %></div>
    <p>Menu</p>
    <div class="nav">
      <ul>
        <% if user_signed_in? %>
          <li>
          <%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %>
          </li>
        <% else %>
          <li>
          <%= link_to 'Login', new_user_session_path %>
          </li>
          <% end %>
      </ul>
    </div>
  </div>
</header>